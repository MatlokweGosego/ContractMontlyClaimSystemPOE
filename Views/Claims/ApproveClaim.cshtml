@model List<ContractMontlyClaimSystemPOE.Models.Claim>
@{
    ViewData["Title"] = "Approve Claims";
}

<h1>Approve Claims (Academic Manager)</h1>
<hr />

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}

@if (Model == null || !Model.Any())
{
    <div class="alert alert-info">
        <h4>No claims for approval</h4>
        <p>There are no pre-approved claims awaiting final approval at this time.</p>
    </div>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>Claim ID</th>
                <th>Lecturer</th>
                <th>Module</th>
                <th>Hours</th>
                <th>Rate</th>
                <th>Total Amount</th>
                <th>Status</th>
                <th>Document</th>
                <th>Pre-Approved Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var claim in Model.Where(c => c.ClaimStatus == "Pre-Approved"))
            {
                <tr>
                    <td>@claim.ClaimID</td>
                    <td>@claim.Lecturer?.FullNames</td>
                    <td>@claim.ModuleName</td>
                    <td>@claim.NumberOfHours</td>
                    <td>R@(claim.AmountOfRate.ToString("N2"))</td>
                    <td>R@(claim.TotalAmount.ToString("N2"))</td>
                    <td>
                        <span class="badge bg-info">@claim.ClaimStatus</span>
                    </td>
                    <td>
                        @if (!string.IsNullOrEmpty(claim.SupportingDocuments))
                        {
                            <a href="/uploads/@claim.SupportingDocuments" target="_blank" class="btn btn-sm btn-outline-primary">View</a>
                        }
                        else
                        {
                            <span class="text-muted">No document</span>
                        }
                    </td>
                    <td>@claim.CreatingDate.ToString("MMM dd, yyyy")</td>
                    <td>
                        <form asp-action="ApproveClaim" method="post" class="d-inline">
                            <input type="hidden" name="claimId" value="@claim.ClaimID" />
                            <button type="submit" class="btn btn-sm btn-success">Approve</button>
                        </form>
                        <form asp-action="RejectClaim" method="post" class="d-inline">
                            <input type="hidden" name="claimId" value="@claim.ClaimID" />
                            <button type="submit" class="btn btn-sm btn-danger">Reject</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}